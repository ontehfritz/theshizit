<%= form_for([@it, @category,@content, @comment]) do |f| %>
  <% if @comment.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@comment.errors.count, "error") %> sh!z blocked, Fix the following YO!:</h2>
      <ul>
      <% @comment.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= f.hidden_field :content_id %>
  <div>
  <%= f.label :theshiz %><br />
  <%= f.text_area :theshiz %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
