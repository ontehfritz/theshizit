<script>
function showAddDialog(){
	$("#divId").dialog({ title: 'Add Some Shiz' });
   	$("#divId").dialog("open");
   	$("#modalIframeId").attr("src","<%= new_it_category_url(@it) %>");
   	return false;
}

$(document).ready(function() {
		$("#divId").dialog({
			autoOpen : false,
			modal : true,
			height : 400,
			width : 500,
			position: [100,50],
			close : function(event, ui) {
				location.reload(true);
			}
		});
});
</script>
<span><%= @it.message %></span><span style="float:right;">:)<%= link_to "Archives" , it_index_path %>(:</span>
<br />
<% if @it.is_current %>
<input type="button" id="add" value="Add" onclick="return showAddDialog()">
<% end %>
<div id="category">
<%= render :partial => "categories/list", :locals=>{:categories=> @categories, :notifications => @notifications} %> 
</div>

<div id="divId" title="Modal Window">
    <iframe id="modalIframeId" width="100%" height="100%"
    marginWidth="0" marginHeight="0" frameBorder="0" scrolling="no"
    title="Dialog Title">Your browser does not support javascript</iframe>
</div>
